<%= form_with(model: drink, local: true) do |form| %>
  <% if drink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluraize(drink.errors.count, "error") %>prohibited this drink from being saved:</h2>

      <ul>
        <% drink.errors.full_massage.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <% form.label :飲み物 %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <% form.label :量　%>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
